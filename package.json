{
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm --dir web build",
    "test": "pnpm --dir web test",
    "typecheck": "pnpm --dir web typecheck && tsc -p api/tsconfig.json"
  },
  "dependencies": {
    "@clerk/backend": "^2.14.0",
    "@neondatabase/serverless": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
